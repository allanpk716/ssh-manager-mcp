# 批2 · Plan 46/45 命令行等价面运行记录(2026-09-21,agent 代跑)

> 册子:`docs/acceptance/plan-46-45-tui-flows.md`(三层拆解中的 CLI 等价面层;行为层已由测试代证,观感层留 owner)。一次性靶子 = `batch2drill`;SAS 比对经 `SSHMGR_PAIR_ASSUME_SAS=1` 跳过(证据注明,总则纪律)。

## GW2′ `pair --force` 全链含 419 撞墙自愈 — 过(419 触发条件已按实测订正)

- 正常入网 → NUC10 吊销临时码 → `pair --force`:**实测该序不撞 419**——force 从不使用旧码,直接提交新配对请求,批准后新材料原子覆盖(槽四件全新、quarantine 目录随重建清除、无半态残留;服务端同名行翻 active 并换新码)。
- **419 本尊在另一条件下捕获**(C2 恢复过程中撞上,顺手闭环):服务端名字上的码仍 **active** 时以同名入网 → `pairing enroll: device name "batch2drill" is in use on the broker (419) — pick another --instance;或 owner 在 broker 侧执行 sshmgr cache-tokens revoke batch2drill 后重试`——**双路径恢复指引原样吐出**(换名 / owner 吊销后重试),与册子判据③的文案形态一致;按指引第二条(broker 侧吊销)重试 → force + 批准成功 ✓。册子步骤序的触发条件据实订正(撞墙条件 = 码 active,非码吊销)。

## GW3 被拒重试 — 过

- 配对请求提交(队列行含三件套与 SAS、来源地址、hint、剩余时限)→ NUC10 `serve pair reject` → 客户端终态报 `pairing request ended by broker (rejected/expired/410 — terminal for this request)`(退出码 1),**失败路径零残留**(槽目录与码表均无踪迹)→ 重试新请求 → `serve pair approve --profile e2e-profile`(120 秒收尾窗)→ 客户端轮询收凭据、首拉 12/13、配对产物落盘。

## GW4 CLI 回归(pair 全参数形态冒烟) — 过

- 全参数形态(`--instance`/`--url`/`--pin`/`--profile-hint`/`--force`)真机多轮跑通(上述各环节即证据);SAS 跳过警示行如约出现(`!! STUB: SAS comparison SKIPPED`)。

## GW5 `cache instances rm` 真机删除 — 过(幂等报文形态如实登记)

- `instances ls` 行状态面(五件套/年龄/半态标注/DEK 孤儿)实证,批2 全程多次使用。
- `cache instances rm batch2drill --yes`:删除成功,**两件配套提示原样输出**(broker 侧吊销指引 + `--write-mcp` 槽外副本提示 + 使用中进程重启提醒);`ls` 复核行消失、槽目录与 DEK 双根干净。
- **幂等再删**:退出码 0 无报错 ✓,但报文为**同款成功文案**而非册子预期的「报不存在」——形态差异如实登记(幂等性达标,报文形态与册子措辞不一致)。
- broker 侧配套吊销已执行,三代临时码终态全 revoked(码表留档)。

## 顺带登记

- 默认槽 `⚠ 半态槽`(全缺)行 = 今日令牌混用事故的隔离残骸(非本册问题,见 [v0.17.1 部署记录](./2026-09-21-v0.17.1-deploy.md));册子「默认槽 ⚠ 半态豁免语义」条仍留 owner 随 GW5 观感目验时定夺。
- 观感层(picker ★、中文列对齐、两级表单观感)留 owner 人工保留面,未动。

## 判定总表

| 项 | 判定 | 一句话 |
|---|---|---|
| GW2′ force/419 自愈 | **过(触发条件订正)** | 419 双路径指引实证(码 active 撞名时);force 原子覆盖无半态 |
| GW3 被拒重试 | **过** | 终态报文 + 零残留 + 重试全链成功 |
| GW4 CLI 冒烟 | **过** | 全参数形态多轮真机跑通 |
| GW5 instances rm | **过(报文形态登记)** | 双配套提示 + 双根干净 + 幂等 exit 0(文案非「报不存在」) |
